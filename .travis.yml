language: php
php:
  #- "5.5"
  #- "5.4"
  #- "5.3"
  - "5.2"

before_install:
  - sudo apt-get update
  - sudo apt-get install --reinstall language-pack-cs-base language-pack-cs
  - sudo locale-gen cs_CZ.utf8

script:
  - locale -a
  - locale
  - php data/bin/check.php
  - php -dshort_open_tag=Off -dmagic_quotes_gpc=Off  test/unit/security/sfSanitizerTest.php
